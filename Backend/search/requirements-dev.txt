# Dependencias de desarrollo y pruebas — no se instalan en la imagen de producción
# Para instalar en local: pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0,<9.0
pytest-asyncio>=0.24.0,<1.0
