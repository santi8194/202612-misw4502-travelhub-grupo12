name: Database Bootstrap (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    name: Use Terraform database stack
    runs-on: ubuntu-latest
    steps:
      - name: Explain the new flow
        shell: bash
        run: |
          echo "::warning::Logical PostgreSQL roles, databases, and schemas are now managed from Infraestructura/terraform/stacks/database with the cyrilgdn/postgresql provider."
          echo "::warning::Run terraform init/plan/apply for that stack instead of using this deprecated bootstrap workflow."
